'use strict';

describe('myApp.particle module', function() {

  beforeEach(module('myApp.particle'));

  describe('particle view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var particleViewController = $controller('particleViewController');
      expect(particleViewController).toBeDefined();
    }));

  });
});